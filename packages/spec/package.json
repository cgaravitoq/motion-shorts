{
  "name": "@cgaravitoq/spec",
  "version": "0.1.0",
  "description": "Canonical Effect Schema domain models for the shorts pipeline: scene-spec, scene-type manifests, and the tagged error taxonomy. In-source TypeScript exports — no compile step.",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "effect": "4.0.0-beta.78"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
