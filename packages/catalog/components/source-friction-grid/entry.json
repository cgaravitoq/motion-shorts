{
  "id": "source-friction-grid",
  "type": "block",
  "status": "first-class",
  "intentTags": [
    "informative",
    "workflow",
    "source"
  ],
  "sourceDemo": "apps/hyperframe/src/episodes/source-driven-catalog-demo/index.html",
  "snippetPaths": [
    "packages/catalog/snippets/source-friction-grid.html"
  ],
  "requiredTracks": [
    6
  ],
  "timingGuidance": "5-7s before/after or pain-point scene; reveal cells in a deterministic order and spotlight the chosen source thesis.",
  "dependencies": [
    "source-attribution-strip"
  ],
  "usageRules": [
    "Use for business-process arguments such as stale docs, version drift, low adoption, or long training cycles.",
    "Keep the grid scene-local; do not wrap it in another generic card unless the grid is loose unframed content.",
    "Use source colors only as accents, not as the persistent brand identity."
  ],
  "validationRules": [
    "requires .hf-source-friction-grid",
    "requires .hf-source-attribution-strip",
    "requires tl.set for discrete labels",
    "forbidden data-composition-src"
  ]
}
