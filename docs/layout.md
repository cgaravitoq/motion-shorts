# Directory layout

Annotated tree of the monorepo. App-specific directories are under `apps/hyperframe/`.

A short is a typed `scene-spec.json`; a deterministic assembler turns it into the monolithic `index.html`
(1:1 — identical spec produces identical bytes). `index.html` is **generated, never hand-edited**.

```
motion-shorts/                 (monorepo root)
├── apps/
│   ├── hyperframe/                Vertical 9:16 shorts pipeline.
│       ├── src/
│       │   ├── lib/
│       │   │   ├── theme.css              CSS vars (colors, spacing). Single source of truth.
│       │   │   ├── easings.css            cubic-bezier vars compatible with GSAP.
│       │   │   ├── timeline-helpers.js    counter, staggerIn, fitText. Loads AFTER gsap.min.js.
│       │   │   └── captions-karaoke.js    TikTok-style karaoke. Loads AFTER timeline-helpers.
│       │   └── episodes/
│       │       └── <slug>/
│       │           ├── scene-spec.json        Typed source of truth (scenes + slots). Hand-authored.
│       │           ├── index.html             Monolithic single-file, GENERATED from scene-spec.json (never hand-edit).
│       │           ├── meta.json
│       │           ├── hyperframes.json
│       │           ├── lib -> ../../lib       Symlink for theme/easings/helpers/captions.
│       │           └── assets/
│       │               ├── voice.mp3          Generated by `bun run audio`.
│       │               ├── captions.json      Hyperframes-shape: [{text, start, end, confidence?}].
│       │               └── generated/         Optional generated PNG/WebP source assets for visual-heavy scenes.
│       ├── templates/                Scene-hub: the building blocks the assembler reads.
│       │   ├── _shell/                Universal look (wraps every episode).
│       │   │   ├── shell.css          Tokens, background layers, brand-corner watermark styles.
│       │   │   └── shell.html.tmpl    Shell template: bg layers, brand corner, single paused GSAP timeline +
│       │   │                          crossfades, captions/audio, track allocation, registry.
│       │   └── scenes/<type>/v1/      One dir per scene-type (hook, title-cards, flow, metric, big-stat,
│       │       │                      comparison, timeline, quote, code, social-card, outro).
│       │       ├── manifest.json      Scene-type contract: slots, ranges, required fields.
│       │       ├── fragment.html      HTML fragment for the scene.
│       │       ├── styles.css         Scene-scoped CSS.
│       │       ├── timeline.js        Seek-safe GSAP timeline fragment.
│       │       └── sample.json        Sample spec for the scene-type (used by the gallery).
│       ├── scripts/
│       │   ├── lib/
│       │   │   ├── scene-instantiator.mjs   Renders one scene-type v1 against its spec slots.
│       │   │   ├── assemble-episode.mjs      Assembles shell + scenes into the monolithic index.html.
│       │   │   ├── scene-spec.mjs            scene-spec.json schema + validation against manifests.
│       │   │   └── scene-router.mjs          Intent -> recommended scene-types mapping.
│       │   ├── assemble.mjs           `bun run assemble <slug>`: regenerate index.html from scene-spec.json.
│       │   ├── scene-qa.mjs           `bun run scripts/scene-qa.mjs <slug> [--scenes=id1,id2]`: per-scene
│       │   │                          visual QA (key-frame snapshots + hyperframes inspect). No full mp4.
│       │   ├── scene-check-all.mjs    `bun run scene:check [<spec>...]`: validate scene-spec(s), no assembly.
│       │   ├── build-catalog-demo.mjs `bun run scene:gallery`: gallery episode exercising every scene-type.
│       │   ├── generate-audio.mjs     `bun run audio`: TTS + STT pipeline.
│       │   ├── render-episode.mjs     `bun run render:episode <slug> --format=mp4 [--keep-local]`.
│       │   ├── new-episode.mjs        `bun run new:episode <slug> [--intent=…]`: scaffold spec + assemble.
│       │   └── dev.mjs                `bun run dev [dir]`: hyperframes preview wrapper.
│       ├── examples/                Script .txt files (input to `bun run audio`).
│       ├── public/voice/<slug>/     Canonical per-episode audio assets (mp3 + captions.json). Gitignored.
│       ├── samples/                 Voice library samples for picking presets. Gitignored.
│       ├── out/                     Auxiliary CLI working copies. Gitignored.
│       ├── renders/                 Local render cache (`--keep-local`) + `<slug>-qa/` scene-QA output. Gitignored.
│       ├── docs/specs/              App-specific spec drafts.
│       ├── hyperframes.json         `paths.episodes: "src/episodes"` — relative to app cwd.
│       ├── package.json             @cgaravitoq/hyperframe (private, dep: @cgaravitoq/audio).
│       └── tsconfig.json            extends ../../tsconfig.json.
│   └── mcp/                       Local stdio MCP server; tools call repo packages directly.
├── packages/
│   ├── audio/                       TTS + STT + ffprobe + script pacing (@cgaravitoq/audio).
│   │   ├── src/
│   │   │   ├── index.ts             Public exports (in-source, no compile step).
│   │   │   ├── elevenlabs.ts, factory.ts, types.ts                TTS.
│   │   │   ├── stt-elevenlabs-scribe.ts, stt-factory.ts, stt-types.ts, stt-hyperframes-transcribe.ts  STT.
│   │   │   ├── ffprobe.ts           getAudioDurationSeconds().
│   │   │   ├── script-pacing.ts     Model-safe pause injection.
│   │   │   └── __tests__/           vitest specs.
│   │   ├── package.json             @cgaravitoq/audio (private, in-source: main/types/exports -> ./src/index.ts).
│   │   └── tsconfig.json            extends ../../tsconfig.json.
│   └── r2-client/                   R2 upload/hydration client (@cgaravitoq/r2-client).
│       ├── src/
│       │   ├── index.ts             Public exports.
│       │   ├── env.ts               Env reads (no process.env outside env.ts).
│       │   ├── sign.ts              Request signing.
│       │   ├── transport.ts         Gateway + direct-S3 transports.
│       │   └── __tests__/           vitest specs.
│       ├── package.json             @cgaravitoq/r2-client (private).
│       └── tsconfig.json            extends ../../tsconfig.json.
├── docs/                            Cross-cutting documentation (rules, layout, decisions).
│   └── decisions/                   Historical design decisions (one file per decision).
├── .agents/skills/                  Canonical skill source (audio-pipeline, canonical-short, new-episode, produce-from-notion).
├── .claude/skills/                  Symlinks -> .agents/skills/<name>.
├── .codex/skills/                   Symlinks -> .agents/skills/<name>.
├── .opencode/skills/                Symlinks -> .agents/skills/<name>.
├── .mcp.json, .codex/config.toml, .opencode/opencode.json   Notion MCP (hosted, OAuth).
├── .env / .env.example              Audio keys and local defaults (.env gitignored).
├── biome.json, bunfig.toml          Shared tool configs.
├── tsconfig.json                    Base TS config (no `include`, `noEmit: true`).
├── turbo.json                       typecheck/test/build/dev/render:episode pipeline.
├── package.json                     workspaces: ["apps/*","packages/*"], scripts delegate via turbo.
└── bun.lock                         Single lockfile.
```

> **Monolithic single-file, generated.** Production shorts use ZERO `data-composition-src`.
> The runtime force-applies `position: absolute; top:0; left:0; 100%x100%` on tracked stage
> children, which collapses any sub-comp internal flex layout. The assembler always emits one
> flat `index.html` per episode; `scene-spec.json` is the only thing you edit.

## Scene-hub model

| Concept | Where |
|---------|-------|
| Episode source of truth | `apps/hyperframe/src/episodes/<slug>/scene-spec.json` |
| Generated render input | `apps/hyperframe/src/episodes/<slug>/index.html` (never hand-edit) |
| Universal shell | `apps/hyperframe/templates/_shell/{shell.css,shell.html.tmpl}` |
| Scene-type building blocks (11) | `apps/hyperframe/templates/scenes/<type>/v1/` |
| Assembler engine | `apps/hyperframe/scripts/lib/{scene-instantiator,assemble-episode,scene-spec,scene-router}.mjs` |

Scene-types (the only building blocks): `hook`, `title-cards`, `flow`, `metric`, `big-stat`,
`comparison`, `timeline`, `quote`, `code`, `social-card`, `outro`. Repeatable slots carry ranges
(e.g. `title-cards.cards` 2-6, `flow.steps` 2-6, `metric.stats` 1-4, `comparison.left/rightPoints` 1-5,
`timeline.events` 3-6, `code.lines` 1-12). `outro` is the pinned brand sign-off — always last, fixed track 7.

## Key paths summary

| Path | Description | Git |
|------|-------------|-----|
| `apps/hyperframe/src/episodes/<slug>/scene-spec.json` | Typed episode source of truth | committed |
| `apps/hyperframe/src/episodes/<slug>/index.html` | Monolithic episode, generated by `bun run assemble` | committed |
| `apps/hyperframe/src/episodes/<slug>/meta.json` | Episode metadata (tail, brand, dimensions) | committed |
| `apps/hyperframe/src/episodes/<slug>/assets/voice.mp3` | Voiceover (hydrate from R2 when remote-only) | committed only for local/source episodes |
| `apps/hyperframe/src/episodes/<slug>/assets/captions.json` | Word-level captions (hydrate from R2 when remote-only) | committed only for local/source episodes |
| `apps/hyperframe/src/episodes/<slug>/assets/generated/` | Generated raster source assets used by the episode HTML | committed when required for reproducible render |
| `apps/hyperframe/templates/_shell/` | Universal shell (tokens, bg, brand corner, timeline) | committed |
| `apps/hyperframe/templates/scenes/<type>/v1/` | Scene-type contracts + fragments | committed |
| `apps/hyperframe/public/voice/<slug>/` | Canonical audio assets | gitignored (regenerable) |
| `apps/hyperframe/renders/` | Local render cache + `<slug>-qa/` scene-QA output | gitignored |
| `apps/hyperframe/out/` | Working copies for render | gitignored |
| `.env` | API keys | gitignored |

## Remote artifacts

R2 plus `render.remote.json` / `assets.remote.json` manifests are the canonical persistence layer for final accepted render and media artifacts. Review renders stay local by default; pass `--upload=r2` only when the output should be persisted remotely. After verified upload, local render outputs are deleted by default unless `--keep-local` is passed. Fresh clones of episodes whose assets live only in R2 should run `bun run hydrate:episode <slug>` from `apps/hyperframe/` before previewing or rendering. Gateway transport (`R2_UPLOAD_GATEWAY_URL` + `R2_UPLOAD_GATEWAY_TOKEN`) is sufficient for both upload and hydration; direct-S3 write keys are an alternative.
