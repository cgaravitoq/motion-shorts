{
  "id": "before-after@1",
  "type": "before-after",
  "version": 1,
  "label": "Before / after wipe",
  "description": "Visual diff: two stacked images inside one self-framed media frame. The AFTER image sits on top of the BEFORE and is revealed by a seek-safe vertical wipe, with a moving divider line and two corner label chips. Optional eyebrow + title.",
  "builder": "build_beforeAfter",
  "classPrefix": "ba",
  "defaultDuration": 7,
  "intentTags": ["informative", "data", "social", "brand", "vfx"],
  "slots": {
    "eyebrow": { "kind": "text", "required": false, "default": "" },
    "title": { "kind": "richText", "required": false, "default": "" },
    "imageBefore": { "kind": "image", "required": true },
    "imageAfter": { "kind": "image", "required": true },
    "labelBefore": { "kind": "text", "required": false, "default": "antes" },
    "labelAfter": { "kind": "text", "required": false, "default": "después" }
  },
  "validation": []
}
