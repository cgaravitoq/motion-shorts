{
  "id": "media-split@1",
  "type": "media-split",
  "version": 1,
  "label": "Media split (copy + screenshot)",
  "description": "Real product copy paired with a screenshot or image. A title and 2-4 marked bullet points lead, with the image as the primary asset in a full-width mid-band. The image carries presence (rounded, bordered, soft shadow) and is never double-framed.",
  "builder": "build_mediaSplit",
  "classPrefix": "ms-",
  "defaultDuration": 7.5,
  "intentTags": ["informative", "social", "brand", "workflow"],
  "slots": {
    "eyebrow": { "kind": "text", "required": false, "default": "" },
    "title": { "kind": "richText", "required": true },
    "image": { "kind": "image", "required": true },
    "mediaSide": { "kind": "text", "required": false, "default": "right" },
    "points": {
      "kind": "repeat",
      "min": 2,
      "max": 4,
      "item": {
        "text": { "kind": "text", "required": true }
      }
    }
  },
  "validation": []
}
