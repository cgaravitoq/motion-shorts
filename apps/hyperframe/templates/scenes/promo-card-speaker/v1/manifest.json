{
  "id": "promo-card-speaker@1",
  "type": "promo-card-speaker",
  "version": 1,
  "label": "Promo card + speaker (light)",
  "description": "Story-ad on a white frame: a rounded speaker photo card in the upper half, a pill badge top-left, the speaker name/role top-right, and a lower block (brand logo, display title, body, solid pill CTA) in dark-on-light. Built for 1:1 replicas of social story ads.",
  "builder": "build_promoCardSpeaker",
  "classPrefix": "pcs",
  "defaultDuration": 4.5,
  "intentTags": ["social", "brand"],
  "slots": {
    "badge": { "kind": "text", "required": true },
    "speakerName": { "kind": "text", "required": true },
    "speakerRole": { "kind": "text", "required": true },
    "title": { "kind": "richText", "required": true },
    "body": { "kind": "richText", "required": true },
    "cta": { "kind": "text", "required": true },
    "photoImage": { "kind": "image", "required": true },
    "logoImage": { "kind": "image", "required": true }
  },
  "validation": []
}
